{% set cardList = paragraph.getComponent() %}
{% set classes = [
  'nsw-section',
  'cards',
  cardList.use_background_color ? 'nsw-bg--off-white',
  paragraph.getPaddingControlClass
] %}
<section{{ attributes.addClass(classes) }}>
  <div class="nsw-container">
    {% include '@dphi_base_theme/components/component--card-list.html.twig' %}
  </div>
</section>
